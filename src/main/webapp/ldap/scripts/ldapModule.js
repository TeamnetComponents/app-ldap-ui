var ldapModule=angular.module('ldapModule',['ldapControllers','ldapServices','ldapDirectives','ldapConstants']);
ldapModule
    .config(function ($routeProvider, $httpProvider, $translateProvider, tmhDynamicLocaleProvider,AUTH_BOOTSTRAP) {
        console.log('ldap Module loading!!!');

    //place some routes here

    });